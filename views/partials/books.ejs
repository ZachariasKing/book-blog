<p>Welcome to the Book Blog!</p>
<% if (books && books.length > 0) { %>
    <ul class="book-list">
        <% books.forEach(book => { %>
            <li class="li-book-item">
                <div class="book-details">
                    <h2 class="book-title"><%= book.title %></h2>
                    <p class="book-authors">Author(s): <%= book.authors.map(author => author.name).join(', ') %></p>
                    <p class="book-publication-date">Publication Date: <%= book.publicationDate.toDateString() %></p>
                    <p class="book-isbn">ISBN: <%= book.ISBN %></p>
                </div>
                <img class="book-cover-img" src="<%= `https://covers.openlibrary.org/b/isbn/${book.ISBN}-M.jpg` %>" alt="<%= book.title %>">
            </li>
        <% }) %>
    </ul>
<% } else { %>
    <p>No books found.</p>
<% } %>